name: flutter_ai_kit
description: A universal AI capability kit for Flutter supporting local models and cloud APIs
version: 1.0.0
homepage: https://github.com/TNT-Likely/BeeCount

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
